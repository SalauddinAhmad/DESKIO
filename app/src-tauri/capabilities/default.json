{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the DESKIO window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
