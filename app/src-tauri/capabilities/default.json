{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the BHUninstaller window.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
